[pytest]
testpaths = tests
# The cross-process concurrency test spawns a dozen interpreters and takes a
# few seconds, so it stays out of the default run. `pytest -m slow` overrides
# this (a later -m wins over addopts) and runs only those.
addopts = -m "not slow"
markers =
    slow: spawns real processes; excluded from the default run
