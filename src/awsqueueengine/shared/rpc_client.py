"""Client-side helper for invoking the host RPC over SSH.

Phase 2 fills this in. Until then, client commands still proxy ad-hoc CLI
invocations over SSH (see :mod:`awsqueueengine.client.remote_query`).
"""
